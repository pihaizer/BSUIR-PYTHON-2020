from setuptools import setup

setup(name='explosion', 
      version='1.0',
      description='Set of some usefool tools.',
      author='explosion204',
      packages=['explosion'],
      test_suite='test_suite',
      zip_safe=False
      )